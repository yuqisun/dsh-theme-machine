# dsh-theme-machine bundle layer: one Loader row pointing at the package's host
# half (lib/index.js). The browser half (lib/client.js) is discovered through
# the dsh.client manifest in package.json, not through this patch.
- insert:
    - id: theme-machine
      name: 'dsh-theme-machine'
